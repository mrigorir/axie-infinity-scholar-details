import styled from 'styled-components';

const Header = styled.div`
  color: var(--color-main-title);
  text-align: center;
  font-weight: bold;
  font-size: 25px;
  padding: 10px;
  margin: 50px auto;
  width: 40%;
  border-radius: 20px;
  box-shadow: 5px 5px 5px var(--shadow-main-title);
`;

export default Header;
