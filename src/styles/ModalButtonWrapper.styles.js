import styled from 'styled-components';

const ModalButtonWrapper = styled.div`
  display: none;
  .track-button-modal {
    padding: 5px 60px 5px 60px;
    font-size: 22px;
    background-color: var(--trackButtonModal);
  }
`;

export default ModalButtonWrapper;
